import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.jstudio.precoshopee',
  appName: 'Calculadora de Preço Shopee',
  webDir: 'dist',
  server: {
    androidScheme: 'https',
  },
};

export default config;
